# Conda Environment Setup 

This project uses **Conda** for Python environment management to ensure reproducibility across local, cloud and GPU-enabled systems. 

Environmental dependencies and definition provided in `environment.yml`. Environment installation provided in `README.md`. 